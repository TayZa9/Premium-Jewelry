export const metadata = {
  title: 'Admin | AURA',
  description: 'Admin command center for AURA luxury jewelry',
};

export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
